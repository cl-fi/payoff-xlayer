# Optional override for read-only probes and local fork tests.
# Pass this variable through your shell; no dotenv loader is used.
XLAYER_RPC_URL=https://rpc.xlayer.tech

# No private key is needed for build, tests, fork, probes, or the offline quote vector.
