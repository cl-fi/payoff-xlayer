# Run the local commands from the repository root. No signing/private key belongs here.
GATEWAY_CONFIG_PATH=gateway/config.local.json
DATABASE_URL=postgresql://payoff:REPLACE_WITH_PASSWORD@127.0.0.1:5432/payoff
XLAYER_RPC_URL=https://rpc.xlayer.tech
SELF_DEALER_API_TOKEN=REPLACE_WITH_SHARED_API_TOKEN
HOST=127.0.0.1
PORT=8080
NODE_ENV=production
